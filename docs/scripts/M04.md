Module 4 — Trial Balance (≈3:15)
Avatar: Black presenter (friendly, calm)

On-screen: “Balance check”
Trial balance checks if total debits = total credits.

On-screen: “Totals must match”
If totals don’t match, there is an error.

On-screen: “Catches vs misses”
Catches posting and adding errors.
Misses missing transactions and wrong account with right amount.

On-screen: “Recap”
Trial balance checks the books.
